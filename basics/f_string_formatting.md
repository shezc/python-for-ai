# f-string 格式化详解：`:.2f`

## 🔍 第7行代码分析

```python
print(f"平均分: {sum(scores) / len(scores):.2f}")
```

这里的 `:.2f` 是 f-string 中的**格式化说明符**（format specifier）。

## 📝 语法结构

```
{表达式:格式说明符}
```

在 `:.2f` 中：
- `:` - 分隔符，表示后面是格式说明符
- `.2` - 保留2位小数
- `f` - 浮点数格式（fixed-point notation）

## 💡 详细解释

### `.2` - 小数位数

- `.2` 表示保留 **2 位小数**
- 如果原数小数位少于2位，会补0
- 如果原数小数位多于2位，会四舍五入

### `f` - 浮点数格式

- `f` 表示 **fixed-point**（定点数）格式
- 总是显示小数部分（即使为0）

## 📊 示例对比

```python
# 原始值
average = 87.625

# 不同的格式化方式
print(f"{average}")           # 87.625（原始值）
print(f"{average:.2f}")      # 87.62（保留2位小数）
print(f"{average:.1f}")      # 87.6（保留1位小数）
print(f"{average:.0f}")      # 88（保留0位小数，四舍五入）
print(f"{average:.3f}")      # 87.625（保留3位小数）
```

### 实际运行示例

```python
scores = [85, 92, 78, 96, 88, 90, 85, 87]
average = sum(scores) / len(scores)

print(f"原始值: {average}")        # 原始值: 87.625
print(f"保留2位: {average:.2f}")  # 保留2位: 87.62
print(f"保留1位: {average:.1f}")  # 保留1位: 87.6
print(f"保留0位: {average:.0f}")  # 保留0位: 88
```

## 🎯 常用格式化选项

### 数字格式化

```python
number = 1234.5678

# 浮点数
print(f"{number:.2f}")    # 1234.57（保留2位小数）
print(f"{number:.0f}")    # 1235（整数，四舍五入）

# 整数
print(f"{number:.0f}")    # 1235（转为整数）

# 千位分隔符
print(f"{number:,.2f}")   # 1,234.57（带千位分隔符）

# 百分比
ratio = 0.875
print(f"{ratio:.2%}")     # 87.50%（百分比格式）
```

### 对齐和填充

```python
text = "Python"
number = 42

# 对齐
print(f"|{text:<10}|")   # |Python    |（左对齐）
print(f"|{text:>10}|")   # |    Python|（右对齐）
print(f"|{text:^10}|")   # |  Python  |（居中）

# 填充
print(f"|{number:05d}|") # |00042|（用0填充，宽度5）
```

### 科学计数法

```python
large_number = 1234567.89

print(f"{large_number:.2e}")  # 1.23e+06（科学计数法）
print(f"{large_number:.2E}")  # 1.23E+06（大写E）
```

## 🔄 与 Node.js 对比

| Python | Node.js |
|--------|---------|
| `f"{num:.2f}"` | `num.toFixed(2)` |
| `f"{num:.0f}"` | `Math.round(num)` |
| `f"{num:,.2f}"` | `num.toLocaleString('en-US', {minimumFractionDigits: 2})` |

### Node.js 等价写法

```javascript
// Python: f"{average:.2f}"
const average = 87.625;
console.log(average.toFixed(2));  // "87.62"

// Python: f"{number:,.2f}"
const number = 1234.5678;
console.log(number.toLocaleString('en-US', {
    minimumFractionDigits: 2,
    maximumFractionDigits: 2
}));  // "1,234.57"
```

## 📚 完整格式说明符语法

```
{[表达式]:[对齐][符号][#][0][宽度][,][.精度][类型]}
```

### 各部分说明

- **对齐**: `<` 左对齐, `>` 右对齐, `^` 居中
- **符号**: `+` 显示正负号, `-` 只显示负号
- **宽度**: 最小字段宽度
- **精度**: `.数字` 表示小数位数
- **类型**: 
  - `f` - 浮点数
  - `d` - 整数
  - `e` - 科学计数法
  - `%` - 百分比
  - `s` - 字符串

## 🎓 实际应用示例

### 示例 1: 计算平均值

```python
scores = [85, 92, 78, 96, 88, 90, 85, 87]
average = sum(scores) / len(scores)

# 使用 .2f 保留2位小数
print(f"平均分: {average:.2f}")  # 平均分: 87.62
```

### 示例 2: 货币格式

```python
price = 1234.567

print(f"价格: ${price:.2f}")        # 价格: $1234.57
print(f"价格: ${price:,.2f}")      # 价格: $1,234.57（带千位分隔符）
```

### 示例 3: 百分比

```python
ratio = 0.875

print(f"完成度: {ratio:.1%}")      # 完成度: 87.5%
print(f"完成度: {ratio:.2%}")      # 完成度: 87.50%
```

### 示例 4: 进度显示

```python
current = 75
total = 100
percentage = (current / total) * 100

print(f"进度: {percentage:.1f}%")   # 进度: 75.0%
```

## 💡 常见问题

### Q1: 为什么是 `.2f` 而不是 `.2`？

**A:** `f` 表示浮点数格式，确保总是显示小数部分。如果只用 `.2`，Python 不知道这是格式化说明符。

### Q2: 可以保留更多小数位吗？

**A:** 可以！例如：
- `.3f` - 保留3位小数
- `.4f` - 保留4位小数

### Q3: 如何四舍五入到整数？

**A:** 使用 `.0f`：
```python
number = 87.625
print(f"{number:.0f}")  # 88（四舍五入）
```

### Q4: 如何添加千位分隔符？

**A:** 在格式说明符中添加 `,`：
```python
number = 1234567.89
print(f"{number:,.2f}")  # 1,234,567.89
```

## 📝 总结

- **`:.2f`** = 保留2位小数的浮点数格式
- **`.`** = 小数点分隔符
- **`2`** = 保留的小数位数
- **`f`** = 浮点数格式类型

**记忆技巧**：
- `.2` = 点2位（保留2位小数）
- `f` = float（浮点数）

---

**在你的代码中**：
```python
print(f"平均分: {sum(scores) / len(scores):.2f}")
```

这行代码会：
1. 计算平均分：`sum(scores) / len(scores)` = 87.625
2. 格式化为2位小数：87.62
3. 显示：`平均分: 87.62`

